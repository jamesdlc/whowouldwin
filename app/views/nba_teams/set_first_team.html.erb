<div class="panel-group" id="accordion_recent_topics">
  <div class="panel panel-default">
      <div class="panel-heading text-center">
        <i class="fa fa-bullhorn"></i> <strong> Choose Team 1 </strong>
      </div>
      <div id="recent_topics" class="panel-collapse collapse in">
        <table class="table table-hover table-striped">
          <thead>
          	<tr>
          		<th class="col-md-6">
                <h2>Western Conference</h2>
                <h4 class="division">Pacific</h4>
                <% @nbateams.each do |t| %>
                  <% if t.division == "Pacific" %>
                    <div class="row team-name-logo-row">
                      <div class="col-md-1">
                        <img src="<%= t.img_url %>" class="team-logo" >
                      </div>
                      <div class="col-md-11">
                        <%= link_to t.name, set_second_team_path(t), method: :get %> <br>
                        <div class="label label-default"> <%= t.abbreviation %> </div>
                      </div>
                    </div>
                  <% end %>
                <% end %>
                <p>
                <h4 class="division">Southwest</h4>
                <% @nbateams.each do |t| %>
                  <% if t.division == "Southwest" %>
                    <div class="row team-name-logo-row">
                      <div class="col-md-1">
                        <img src="<%= t.img_url %>" class="team-logo" >
                      </div>
                      <div class="col-md-11">
                        <%= link_to t.name, set_second_team_path(t), method: :get %> <br>
                        <div class="label label-default"> <%= t.abbreviation %> </div>
                      </div>
                    </div>
                  <% end %>
                <% end %>
                <h4 class="division">Northwest</h4>
                <% @nbateams.each do |t| %>
                  <% if t.division == "Northwest" %>
                    <div class="row team-name-logo-row">
                      <div class="col-md-1">
                        <img src="<%= t.img_url %>" class="team-logo" >
                      </div>
                      <div class="col-md-11">
                        <%= link_to t.name, set_second_team_path(t), method: :get %> <br>
                        <div class="label label-default"> <%= t.abbreviation %> </div>
                      </div>
                    </div>
                  <% end %>
                <% end %>
              </th>
          		<th class="col-md-6">
                <h2>Eastern Conference</h2>
                <h4 class="division">Atlantic</h4>
                <% @nbateams.each do |t| %>
                  <% if t.division == "Atlantic" %>
                    <div class="row team-name-logo-row">
                      <div class="col-md-1">
                        <img src="<%= t.img_url %>" class="team-logo" >
                      </div>
                      <div class="col-md-11">
                        <%= link_to t.name, set_second_team_path(t), method: :get %> <br>
                        <div class="label label-default"> <%= t.abbreviation %> </div>
                      </div>
                    </div>
                  <% end %>
                <% end %>
                <h4 class="division">Central</h4>
                <% @nbateams.each do |t| %>
                  <% if t.division == "Central" %>
                    <div class="row team-name-logo-row">
                      <div class="col-md-1">
                        <img src="<%= t.img_url %>" class="team-logo" >
                      </div>
                      <div class="col-md-11">
                        <%= link_to t.name, set_second_team_path(t), method: :get %> <br>
                        <div class="label label-default"> <%= t.abbreviation %> </div>
                      </div>
                    </div>
                  <% end %>
                <% end %>
                <h4 class="division">Southeast</h4>
                <% @nbateams.each do |t| %>
                  <% if t.division == "Southeast" %>
                    <div class="row team-name-logo-row">
                      <div class="col-md-1">
                        <img src="<%= t.img_url %>" class="team-logo" >
                      </div>
                      <div class="col-md-11">
                        <%= link_to t.name, set_second_team_path(t), method: :get %> <br>
                        <div class="label label-default"> <%= t.abbreviation %> </div>
                      </div>
                    </div>
                  <% end %>
                <% end %>
              </th>
          	</tr>
          </thead>
        </table>
      </div>
    <div class="panel-footer clearfix">
      <div class="pull-right">
        &nbsp;
      </div>
    </div>
  </div>
</div>
